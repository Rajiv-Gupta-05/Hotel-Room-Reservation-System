/**
 * Development environment configuration.
 * The Angular CLI replaces this file with environment.prod.ts
 * when building for production (ng build --configuration production).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5001/api',
};
